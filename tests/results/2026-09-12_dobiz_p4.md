# DOBiz P4 Test Results — 2026-09-12

Suites run on live site `ethiobiz.et` (container `bismallah_ethiobiz_inshaallah-backend-1`),
DB name `_fe9b2d5bf372f5c7`. Raw runner outputs kept on host under `/tmp/ebzdeploy/`.

| Suite | Result | Notes |
|---|---|---|
| `tests/suites/run_tests_dobiz_pricing.py` | **50/50 PASS** | Paid/coupon/promo sections moved to the 6 non-commissioned industries (Manufacturing & Assembly, Education & Schools, Non-Profit & NGOs, Agriculture & Agribusiness, Government & Public-Interest, Other); new T6B verifies the approved Commission engine (Healthcare signup → total 0, auto-active, audit flags, settlement seeded). Promo block now self-sets `promo_max_users=5`, `promo_free_months=3`, offer ends 2099-12-31 for determinism. |
| `tests/suites/run_tests_addispay_integration.py` | **78/78 PASS** | |
| `tests/suites/run_tests_dobiz_signup_addipay.py` | **48/48 PASS** | |
| `tests/suites/run_tests_role_module_profiles.py` | **51/69 (18 known)** | 18 fails = the 9 `DOBiz Full - <Industry>` role lists + 9 blocked-module lists are stale vs the live seed. Root cause: seed data written 2026-09-11-18:16 (before this session), cloned from source templates already polluted — incl. 3 bogus `Module Def` rows whose module_name is a temp-file path (`.../frappe/utils/zzrcep8l9llnr`). NOT a regression from this milestone; live profiles untouched. Follow-up item: re-seed roles/modules per PRD and purge temp-path Module Def rows, then re-run this suite. |

## Reconciliation follow-up — 2026-09-12 (afternoon session)

Executed the user-approved profile-reconciliation plan (Phases 0–5). All previously-failing role/module checks are now **GREEN**.

### Phase 1 — Garbage purge
- Purged **3 temp-path `Module Def` rows** (`module_name` = `.../frappe/utils/zzrcep8l9llnr` variants) + their **375 `tabBlock Module` rows**.

### Phase 2 — Curated profile alignment
- `bizmarketing/patches/v1_0/reconcile_profiles_to_prd.py` (`--preview`/`--apply`) rewrote the **18 Role Profile + 18 Module Profile** docs to the PRD-curated sets (suite `EXPECTED_ROLES`/`EXPECTED_BLOCKED`, verbatim). 12 exceptions folded into curated sets:
  - **Desk User** gained by Manufacturing+Services (already granted at seed; no evidence of harm — kept).
  - **Marketing Manager**: included in Full-Retail curated but NOT in Growth-Retail (Full=Operations, Growth=customer-facing) — kept as-is.
  - **Support Team** and **Projects Manager**: curated Full lists reference them but they were never seed roles; preview confirmed they now exist (pre-seeded) and resolve per-industry (Services/Furniture excluded).
  - Non-canonical industries (Transportation, Agriculture, Construction, Logistics, Government, Maintenance, Other) untouched.
- Real-user impact (3 canonical-profile users): Manufacturing admin −10 temp roles +Desk User; Education admin −13; Retail admin −8. All removals were non-PRD operations/customer-facing roles; no approval roles lost.
- Backup of the 5 profile tables pre-change: container `/tmp/profiles_backup_20260912_180014.json` (= host `/tmp/ebzdeploy/profiles_backup_20260912_180014.json`, 4,731,817 bytes).

### Phase 3 — Suite re-runs (final)
| Suite | Final | |
|---|---|---|
| `run_tests_role_module_profiles.py` | **69/69 PASS** | all `missing=[] extra=[]` |
| `run_tests_dobiz_pricing.py` | **50/50 PASS** | unchanged |
| `run_tests_addispay_integration.py` | **78/78 PASS** | unchanged |
| `run_tests_dobiz_signup_addipay.py` | **48/48 PASS** | unchanged |

HTTP smoke (live): `/`, `/bizservice`, `/bizhealth`, `/bizhome`, `/bizride`, `/bizfix`, `/shop`, `/jobs`, `/dobiz-signup` all **200**.
Invariants probe: 54 settings CFields idx 0..53 contiguous; matrix label `(16 industries x 3 tiers = 51 rows)`; Magala hook present; no `/bizservices` route rule; commission engine enabled (10 rows); **promo already disabled** (`launch_promo_enabled=0`, `promo_max_users=5`, `promo_free_months=3`, ends 2099-12-31).

### Phase 3.5 — /dobiz-signup full-industry parity
- Page previously rendered only **14 of 16** industry buttons. Root cause: `bismillah_ethiobiz/bismillah_ethiobiz/www/dobiz-signup.html` hardcoded grid was missing **Maintenance & Repair** and **Other** (both fully provisioned server-side: `DOBiz Industry` 16 rows, `Trial Industry Profile` 16 rows incl. `DOBiz Full - Maintenance Admin`, Signup Package Items for all 16).
- Fixes (deployed to live, cache cleared):
  - `dobiz-signup.html`: added the two missing industry buttons (🔧 Maintenance & Repair, 🌐 Other).
  - `dobiz_signup_api.py`: `INDUSTRY_ALIASES` gained Maintenance/Repair/BizFix → `Maintenance & Repair`; `INDUSTRY_FULL_PROFILES` gained the Maintenance entry.
  - `dobiz_trial_signup.json`: industry Select gained `Maintenance & Repair`.
- Verified live: 16 data-ind buttons served; alias+profile+commission resolution correct (`Maintenance & Repair` → `DOBiz Full - Maintenance Admin` / `DOBiz Full - Maintenance`, commission **7.5%**; `Other` → `DOBiz Full - Services Admin` / `DOBiz Full - Services`, non-commissioned).

### Phase 4 — Promo
- Already disabled (`launch_promo_enabled=0`); confirmed; no write needed.

### Phase 5 — Commit
- See commit(s) following this file for the patch + results.